# Owner decisions for `arrival_year` review

The owner confirmed that `P004849` and `P005024` have no issues. `P003355` also has no issue and is retained as the sole reviewed exception where both `origin_place = На Сахалине` and `arrival_year` are populated.

For `P005199`, `arrival_year = 1887` and `origin_place = Киевская губерния` remain unchanged. The incorrect recognized `age = 2` is corrected to the owner-approved value `25` in a new staged candidate.

An exact and case-insensitive search found no additional records combining a populated `arrival_year` with `origin_place = На Сахалине`.
